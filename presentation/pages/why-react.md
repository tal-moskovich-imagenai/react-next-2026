---
layout: center
clicks: 3
---

## So... why React for a terminal?

<WhyReact :step="$clicks" />

<!--
[click 1] The contrast — console.log (static) vs UI = f(state) (live). Claude Code needs the right one.
[click 2] THE MAIN REASON — Claude Code is a real, stateful app, not a script. That's React.
[click 3] The payoff flow — React + TS is "on distribution" → the agent writes it for you (Boris Cherny)
-->
