# Step 1 — Scaffold

**What you're doing:** Set up the project structure.

```bash
npm install
```

**Files added this step:**
- `package.json` — deps: ink, react, ai, @ai-sdk/openai
- `tsconfig.json` — NodeNext + react-jsx

**Next:** Add `src/index.tsx` and `src/App.tsx` → Step 2

---

**Live demo timing:** ~1 minute
